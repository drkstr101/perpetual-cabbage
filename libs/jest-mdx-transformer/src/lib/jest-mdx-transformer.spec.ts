import { jestMdxTransformer } from './jest-mdx-transformer';

describe('jestMdxTransformer', () => {
  it('should work', () => {
    expect(jestMdxTransformer()).toEqual('jest-mdx-transformer');
  });
});
